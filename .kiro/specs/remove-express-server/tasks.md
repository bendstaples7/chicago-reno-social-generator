# Tasks: Remove Express Server, Consolidate on Cloudflare Workers

- [x] **Task 1: Create new branch off main.** Pull latest main (`git checkout main && git pull`), then create branch `refactor/remove-express-server`. Update root `package.json`: remove `"server"` from workspaces array, remove `dev:server`, `build:server`, and `dev:client:worker` scripts, add `dev:worker` (`npm run dev --workspace=worker`) and `build:worker` (`npm run build --workspace=worker`) scripts. Keep `dev:client` as `npm run dev --workspace=client`.
- [x] **Task 2: Update client vite proxy.** In `client/vite.config.ts`, hardcode the proxy target to `http://localhost:8787` and remove the `VITE_API_TARGET` env var conditional. Remove the `cross-env` devDependency from root `package.json` if no other scripts use it.
- [x] **Task 3: Delete server/ directory.** Delete the entire `server/` directory. Run `npm install` to regenerate `package-lock.json` without server dependencies.
- [x] **Task 4: Create worker/.dev.vars.example.** Create `worker/.dev.vars.example` with all required environment variable keys (no values) — AI_TEXT_API_KEY, FB_PAGE_ACCESS_TOKEN, IG_BUSINESS_ACCOUNT_ID, CHANNEL_ENCRYPTION_KEY, INSTAGRAM_CLIENT_SECRET, INSTAGRAM_CLIENT_ID, JOBBER_CLIENT_ID, JOBBER_CLIENT_SECRET, JOBBER_ACCESS_TOKEN, JOBBER_REFRESH_TOKEN. Verify `worker/.dev.vars` is in `.gitignore`.
- [x] **Task 5: Migrate tests/unit/cross-poster.test.ts.** Rewrite to import `CrossPoster` from `worker/src/services/cross-poster.js`. Replace `pg` query mock with `MockD1Database` from `tests/unit/helpers/mock-d1.ts`. Import `PlatformError` from `worker/src/errors/index.js`. Adapt all test cases to use D1 prepared statement patterns instead of `query()` calls. Verify tests pass.
- [x] **Task 6: Migrate tests/unit/error-formatting.test.ts.** Rewrite to import `PlatformError` and `formatErrorResponse` from `worker/src/errors/`. Rewrite error handler tests to use Hono's `onError` handler from `worker/src/middleware/error-handler.js` instead of Express middleware. Remove `express` type imports. Verify tests pass.
- [x] **Task 7: Migrate property tests.** In `tests/property/auto-data-sync-preservation.property.test.ts` and `tests/property/auto-data-sync-bug-condition.property.test.ts`, change all `readFileSync` paths from `server/src/` to `worker/src/`. Verify structural assertions still pass against worker source files. Run `npm test` to confirm.
- [x] **Task 8: Update steering files.** Rewrite `.kiro/steering/tech.md` — remove Express/PostgreSQL sections, update to three-package monorepo (client, worker, shared), update Common Commands. Rewrite `.kiro/steering/structure.md` — remove server/ from directory layout, update architecture patterns to reference worker only. Delete `.kiro/steering/server-worker-parity.md`. Review `.kiro/steering/product.md` for any server references.
- [x] **Task 9: Clean up agent hook and GitHub Actions.** Delete `.kiro/hooks/server-worker-parity-check.kiro.hook`. Review `.github/workflows/deploy-worker.yml` to verify paths trigger is correct (no changes expected).
- [ ] **Task 10: Final verification.** Run `npm install`, `npm test` (all tests pass), `npm run build:client`, `npm run build:worker`. Grep the codebase for any remaining `server/src` references. Commit and push branch.
  - **Note:** `npm run build:worker` fails with 7 pre-existing TypeScript errors in files not touched by this spec:
    - `src/routes/content-ideas.ts` (2 errors): `string` not assignable to `ContentType` parameter
    - `src/routes/posts.ts` (1 error): `string` not assignable to `ContentType` parameter
    - `src/services/quote-draft-service.ts` (1 error): missing `draftNumber` property on `QuoteDraft`
    - `src/services/quote-engine.ts` (1 error): missing `draftNumber` property on `QuoteDraft`
    - `src/services/quote-sync-service.ts` (2 errors): implicit `any` types on `data` and `connection`
  - All other verification steps pass: `npm install`, `npm test` (163 pass), `npm run build:client`, codebase grep clean.
